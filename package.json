{
    "name": "wasm-share-memory",
    "version": "0.1.0",
    "description": "A utility package that allows to share memory between multiple WASM instances",
    "scripts": {
        "build:wasm": "rimraf pkg && wasm-pack build rust-lib --target web --out-dir ../pkg --out-name rust_module",
        "build": "rimraf dist && tsup",
        "format": "prettier --write ."
    },
    "files": [
        "dist"
    ],
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "devDependencies": {
        "prettier": "^3.6.2",
        "rimraf": "^6.0.1",
        "tsup": "^8.5.0"
    }
}
